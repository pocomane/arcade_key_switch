
use <util.scad>;
use <arcade_key_switch.scad>;

module main(){
  /* slice(thick=0.2,start=[0,0,0]) */
    arcade_key_switch();
}

// --------------------------------------------------------------------------------
main();

